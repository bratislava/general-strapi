/**
 * sluzby-pre-ludi-bez-domova controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::sluzby-pre-ludi-bez-domova.sluzby-pre-ludi-bez-domova');
